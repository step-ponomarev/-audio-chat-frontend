const HOST = "52.234.178.219"

export const URL = `https://${HOST}:8080`

export const socketServer = `ws://${HOST}:8188`;
export const httpServer = `http://${HOST}/janus`;
export const sockJsServer = `http://${HOST}:8080/chat-app`
